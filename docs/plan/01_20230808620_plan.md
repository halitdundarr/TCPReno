# Member Plan - 20230808620

## Responsibility
Core simulator, sender state model, and scenario engine.

## Concrete Tasks
1. Define `SenderState`, `PacketEvent`, `CwndLog`, and topology structs.
2. Implement scenario parser (`step event` format).
3. Implement shared simulation loop and log printer.
4. Ensure deterministic output format for every step.
5. Verify timeout and triple-dup events are injectable from files.

## Deliverables
- `src/tcp_state.h`
- `src/tcp_state.c`
- `src/simulator.h`
- `src/simulator.c`
- `scenarios/*.txt`
