# Member Plan - 20230808617

## Responsibility
`TCP Tahoe` implementation, analysis, and submission presentation assets.

## Concrete Tasks
1. Implement Tahoe reaction to timeout and triple-dup ACK (`cwnd=1` reset).
2. Add and verify topology configs (`A.conf` ... `F.conf`).
3. Run Tahoe scenarios and collect outputs.
4. Compare Reno vs Tahoe with metrics and recovery behavior.
5. Prepare README and demo narration flow.

## Deliverables
- `src/tcp_tahoe.c`
- `topology/A.conf` ... `topology/F.conf`
- `results/tahoe_timeout.txt`
- `results/tahoe_triple_dup.txt`
- `results/tahoe_mixed.txt`
